# How to start demo site: 
1. Using docker: 
    - Run the following command:
           `docker pull gprestes/the-internet`
    - Then:
       `docker run -d -p 7080:5000 gprestes/the-internet` 
2. Go to http://localhost:7080
